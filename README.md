
  # Shareable Design Update

  This is a code bundle for Shareable Design Update. The original project is available at https://www.figma.com/design/xJgDaaRl48prHZZTAnTfBZ/Shareable-Design-Update.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  